spring:
  datasource:
    url: jdbc:mysql://localhost:3306/approval?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root

logging:
  level:
    com.example.approval: DEBUG
    org.springframework.web: DEBUG