# RocketMQ 配置示例
rocketmq:
  name-server: localhost:9876
  producer:
    group: event-producer-group
    send-message-timeout: 3000
    retry-times-when-send-failed: 2

shared:
  event:
    rocketmq:
      enabled: true  # 启用 RocketMQ 分发器
    redis:
      enabled: false  # 可选：启用 Redis 分发器