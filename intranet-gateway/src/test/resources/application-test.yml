spring:
  application:
    name: intranet-gateway

  # 测试环境禁用 Redis 与 sa-token Redis DAO(使用默认内存存储)
  autoconfigure:
    exclude:
      - org.redisson.spring.starter.RedissonAutoConfigurationV2
      - org.redisson.spring.starter.RedissonAutoConfigurationV1
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
      - cn.dev33.satoken.dao.SaTokenDaoForRedisTemplate

  cloud:
    nacos.discovery.enabled: false # 禁止向 Nacos 注册服务
    gateway.server.webflux.discovery.locator.enabled: false

# 内网网关启用 HQ + BRANCH 渠道
gateway:
  channels:
    enabled:
      - HQ
      - BRANCH

## 白名单配置
auth:
  gateway:
    public-paths:
      - /actuator/**
      - /actuator
      - /favicon.ico
      - /hq/auth/login
      - /branch/auth/login
      - /branch/auth/secondary-auth/initiate
      - /branch/auth/secondary-auth/confirm
      - /branch/auth/secondary-auth/status/**