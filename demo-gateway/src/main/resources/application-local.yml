# sofa registry settings
spring:
  cloud:
    nacos.discovery.server-addr: 127.0.0.1:8848 # nacos / tkEAaj3OIk
    gateway.server.webflux:
      discovery.locator:
        enabled: true
        lower-case-service-id: true
      routes:
        # ============ iam-service(三渠道认证授权) ============
        # 网上渠道认证授权
        - id: iam-service-internet-route
          uri: lb://iam-service
          predicates:
            - Path=/internet/auth/**
          filters:
            - StripPrefix=0
            - AddRequestHeader=X-Gateway-Source, demo-gateway
        # 总部渠道认证授权
        - id: iam-service-hq-route
          uri: lb://iam-service
          predicates:
            - Path=/hq/auth/**
          filters:
            - StripPrefix=0
        # 网点渠道认证授权
        - id: iam-service-branch-route
          uri: lb://iam-service
          predicates:
            - Path=/branch/auth/**
          filters:
            - StripPrefix=0
        # iam-service 通用路由(管理接口)
        - id: iam-service-admin-route
          uri: lb://iam-service
          predicates:
            - Path=/iam/**
          filters:
            - StripPrefix=1
        # ============ business-core-kernel(核心业务服务) ============
        - id: business-core-route
          uri: lb://business-core-kernel
          predicates:
            - Path=/business/**
          filters:
            - StripPrefix=1
        # ============ product-service(客户服务) ============
        - id: product-service-route
          uri: lb://product-service
          predicates:
            - Path=/product/**
          filters:
            - StripPrefix=1
        # ============ 其他业务服务 ============
        - id: consumer-service-route
          uri: lb://consumer
          predicates:
            - Path=/consumer/**
          filters:
            - StripPrefix=1
            - AddRequestHeader=X-Gateway-Source, demo-gateway
        - id: integration-service-route
          uri: lb://integration
          predicates:
            - Path=/integration/**
          filters:
            - StripPrefix=1
        - id: approval-service-route
          uri: lb://approval-service
          predicates:
            - Path=/approval/**
          filters:
            - StripPrefix=1
        - id: file-service-route
          uri: lb://file-service
          predicates:
            - Path=/file/**
          filters:
            - StripPrefix=1
        - id: annuity-service-route
          uri: lb://annuity-service
          predicates:
            - Path=/annuity/**
          filters:
            - StripPrefix=1

gateway:
  exclude-routes:
    default-status: 403
    rules:
      - path: /aaa/**
        methods: [ GET, POST ]
        status: 404
      - path: /internal/**

logging:
  level:
    root: INFO
    com.example: INFO
    org.springframework: ERROR
    org.springframework.cloud: ERROR
    org.springframework.cloud.gateway: INFO
    org.springframework.web: INFO
    reactor.netty.http.client: INFO
    reactor.netty.http.server.AccessLog: INFO
